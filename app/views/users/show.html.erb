 <table>
    <thead>
      <tr>
        <th></th>
        <th>Image</th>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= attachment_image_tag user, :profile_image, fallback: "no_imagKBe.jpg" %></td>
        <td><%= user.name %></td>
        <%= link_to "Show", user_path(user.id) %>
      </tr>
    </tbody>
  </table>
  
</div>
